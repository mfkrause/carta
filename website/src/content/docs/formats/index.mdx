---
title: Formats
description: Every format carta reads and writes, with the gaps that remain.
sidebar:
  order: 1
---

import FormatGrid from '../../../components/FormatGrid.astro';

carta names formats the way pandoc does, so a command written for one runs against the other. A
format name may carry extension toggles (`markdown+hard_line_breaks-raw_html`); `carta
--list-extensions=<format>` prints the set a given format accepts.

## What ships today

<FormatGrid />

## The whole surface

The table below includes formats that are not started yet, so the roadmap is visible alongside what
works. `➖` means the direction does not apply: there is no such reader or writer to build.

<FormatGrid shippingOnly={false} />

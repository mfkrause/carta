.. role:: custom

.. role:: highlight(emphasis)

.. role:: tagged
   :class: alert important

.. role:: based(strong)
   :class: ignored

A :custom:`bare custom role` and a :highlight:`role with a base`.

A :tagged:`role carrying its own classes` and a :based:`base that wins over a class list`.

A paragraph with *emphasis*, **strong emphasis**, and ``inline literal`` text.

A second paragraph that spans
two source lines joined by a soft break.

Escapes keep a literal \*asterisk\* intact, and an inline literal can attach
to a word like with``code`` mid-sentence.

A statement needing support @missing-key here.

Grouped keys @first-key @second-key in sequence.

A call form #cite(<third-key>) in running text.

A call form #cite(label("fourth-key")) built from a label call.

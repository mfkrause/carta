Inline math $x^2 + y^2 = z^2$ inside a sentence.

$ sum_(i=1)^n i = (n(n+1))/2 $

Fractions and roots: $frac(a, b)$, $sqrt(x)$, $root(3, x)$.

Matrices:

$ mat(1, 2; 3, 4) $

Cases:

$ cases(x "if" y, z "otherwise") $

#figure(
  image("logo.png", width: 40%),
  caption: [A pictured caption.],
)

#figure(
  table(columns: 2, [x], [y]),
  caption: [A tabular caption.],
)

An inline #image("inline.png") in a paragraph.

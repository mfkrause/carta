Before #figure(image("logo.png"), caption: [A caption.]) after.

Text #table(columns: 2, [a], [b]) more text.

Lead #heading[A heading] trail.

- item #list([nested]) tail

Quoted #quote(block: true)[A quotation.] onward.

Code #raw(block: true, "let x = 1") onward.

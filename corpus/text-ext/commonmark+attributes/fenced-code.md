A fenced code block with a full attribute spec:

~~~ {#example .rust .numberLines startFrom="100"}
fn main() {}
~~~

A bare language string still works:

~~~ rust
let x = 1;
~~~

An attribute block with key-value pairs and a quoted value:

``` {.python data-line="3 4 5"}
print("hi")
```

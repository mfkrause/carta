# Folding fences

A closed code fence ends a paragraph and opens a block of its own:
```
this text is code
```

An unterminated fence is different: with no closing line it opens no code
block, so its delimiter and the lines beneath it stay part of the paragraph:
```
this line is prose, not code

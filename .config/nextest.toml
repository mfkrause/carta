# The default 100ms leak-timeout trips on process-teardown jitter and flags in-process tests as
# leaky; a real leak would hold the stdio pipes far longer than 500ms.
[profile.default]
leak-timeout = "500ms"
